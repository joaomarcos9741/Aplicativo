import { StyleSheet } from "react-native"

export const colors = {
    black: '#000',
    white: '#FFF',
    cor_fundo: 'rgba(255,255,255,0.5)'

}

export const styleContainer = StyleSheet.create({
    container: {
        flex: 1,
    }

}
)